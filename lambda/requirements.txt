# Lambda worker dependencies.
# boto3 is preinstalled in the Lambda Python 3.11 runtime, but we pin it for
# local packaging consistency. Install everything into the deployment zip.
requests==2.32.3
PyJWT[crypto]==2.10.1
openai==1.59.6
boto3==1.35.90
